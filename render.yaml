services:
  - type: web
    name: flydronics-website
    env: node
    plan: free
    nodeVersion: 22.16.0
    buildCommand: npm ci --legacy-peer-deps && npm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
